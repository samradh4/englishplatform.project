{
  "name": "bolo-english",
  "version": "20.0.0-pricing-sqlite-persistent-login",
  "private": true,
  "description": "Bolo English with instant access, 1-day trial, paid WhatsApp plans, SQLite persistence, saved multi-device login, open room access for active members, voice rooms, and admin controls.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/js/common.js && node --check public/js/auth.js && node --check public/js/admin.js && node --check public/js/dashboard.js && node --check public/js/room.js && node --check public/js/public-rooms.js && node --check public/js/pending.js && node --check public/js/private-session.js"
  },
  "engines": {
    "node": ">=22"
  }
}
